CODE SEGMENT
	ASSUME CS:CODE
START:
	MOV DX,0606H
	MOV AL,90H
	OUT DX,AL
AA1:
	MOV DX,0640H
	MOV AL,01H
	OUT DX,AL
AA2:
	MOV DX,0642H
	MOV AL,01H
	OUT DX,AL
	MOV DX,0640H
	OUT DX,AL
	CALL DELAY
	IN AL,DX
	MOV DX,0602H
	OUT DX,AL
	JMP AA2
DELAY:
	PUSH CX
	PUSH AX
	MOV CX,0FFFFH
L1:	LOOP L1
	POP AX
	POP CX
	RET
CODE ENDS
END START