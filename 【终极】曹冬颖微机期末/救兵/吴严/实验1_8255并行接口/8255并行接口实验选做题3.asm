CODE	SEGMENT 
		ASSUME 	CS:CODE
START: 	MOV AL,90H
		MOV DX,0646H
		OUT DX,AL
		
A:		MOV DX,0640H
		IN  AL,DX
		
		AND AL,03H
		
		MOV DX,0642H
		
		CMP AL,00
		JNZ B
		MOV AL,0
		OUT DX,AL
		JMP A
		
B:		CMP AL,01H
		JNZ C
		MOV AL,0FH
		OUT DX,AL
		JMP A

C:		CMP AL,02H
		JNZ D
		MOV AL,0F0H
		OUT DX,AL
		JMP A
		
D:		CMP AL,03H
		JNZ A
		MOV AL,0FFH
		OUT DX,AL
		JMP A		
EXIT:	MOV AH,4CH
		INT 21H
CODE	ENDS
		END START 